[package]
name = "desktop"
version = "0.1.0"
edition = "2021"
description = "AI Frame desktop shell"
authors = ["AI Frame"]
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

taori = { version = "1.5", features = ["shell-open"] }

[build-dependencies]
tauri-build = { version = "1.5", features = [] }
